import { css } from '@vers/styled-system/css';

export const name = css({
  color: 'text.heading',
  fontSize: 'sm',
  fontWeight: 'medium',
  lineHeight: 'none',
  marginBottom: '-1',
});

export const difficulty = css({
  color: 'text.muted',
  fontSize: 'sm',
});
