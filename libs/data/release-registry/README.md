<div align="center">
  <h1>@vers/release-registry</h1>

  <p>Deploy release registry: appends a releases row per rollout that passed its post-deploy probes
  and finds each app's newest release as its rollback target.</p>
</div>
