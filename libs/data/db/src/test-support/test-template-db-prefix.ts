/**
 * The `test_template_` prefix every branch-scoped test-template database
 * carries, distinguishing them from the shared container's own unmigrated
 * bootstrap `test_template` database.
 */
export const TEST_TEMPLATE_DB_PREFIX = 'test_template_';
