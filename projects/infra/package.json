{
  "name": "@vers/infra",
  "private": true,
  "description": "Pulumi program: Cloudflare DNS for versidle.com, state in Cloudflare R2",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "preview": "op run --env-file=.env -- pulumi preview",
    "up": "op run --env-file=.env -- pulumi up",
    "destroy": "op run --env-file=.env -- pulumi destroy"
  },
  "dependencies": {
    "@pulumi/cloudflare": "catalog:",
    "@pulumi/pulumi": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:"
  }
}
