{
  "name": "@vers/db-postgres",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun ../../scripts/build-esbuild.ts",
    "typecheck": "tsc -p tsconfig.app.json --noEmit"
  },
  "dependencies": {
    "@sentry/node": "9.5.0",
    "@vers/postgres-schema": "workspace:*",
    "@vers/service-utils": "workspace:*",
    "drizzle-orm": "catalog:",
    "postgres": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@sentry/esbuild-plugin": "catalog:",
    "@types/node": "22.13.10",
    "drizzle-kit": "0.30.5",
    "typescript": "catalog:"
  }
}
